{% extends "base.html" %}
{% block content %}
{% for post in posts %}
<h2><a href='article/1'>{{ post.title }}</a></h2>
<p>{{ post.timestamp }}</p>
<p>{{ post.body }}</p>
{% endfor %}    
{% endblock %}
